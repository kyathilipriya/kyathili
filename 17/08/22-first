#include <stdio.h>

int main() {
    int i,count=0,a[]={1,2,3,6};
    printf("series is:\n");
    for(i=0;i<5;i++)
{
    if(a[i]==a[i]+1)
    count++;
    else{
        a[i]=a[i-1]+1;
        printf("%d\n",a[i]);
    }
}
    return 0;
}
